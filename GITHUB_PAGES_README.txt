MEMORA v14 - GitHub Pages QR fixed package

Fixes included:
1. QR links now use only ?archive=... so phone scanners do not lose the result data after &.
2. Archive pages now open when the URL has archive= even without detail=.
3. GitHub Pages path /em222/ is kept with a trailing slash to avoid assets resolving to /assets.
4. .nojekyll is included for GitHub Pages.

Upload the CONTENTS of this folder to your em222 repository root.
The root of the repo should contain index.html and assets/.
Do not upload the folder as an extra nested folder.
